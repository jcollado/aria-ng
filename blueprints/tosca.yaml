tosca_definitions_version: tosca_simple_yaml_1_0

description: |
  A demo of a TOSCA blueprint.

imports:
  - tosca-types.yaml

topology_template:

  node_templates:

    my_node:
      description: |
        My node template.
      type: MyNode
